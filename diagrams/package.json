{
  "name": "pathfinder-diagrams",
  "version": "1.0.0",
  "description": "Static diagram generator for PathFindR presentation",
  "main": "generate-images.js",
  "scripts": {
    "generate": "node generate-images.js",
    "install-deps": "npm install playwright",
    "setup": "npm run install-deps && npx playwright install chromium"
  },
  "keywords": [
    "pathfinder",
    "diagrams",
    "mermaid",
    "static-generation"
  ],
  "author": "PathFindR Team",
  "license": "MIT",
  "devDependencies": {
    "playwright": "^1.40.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}